__pycache__/
*.pyc
.DS_Store
bin/
/sessions
coverage.out
dist/

# local secrets / runtime config - never commit real credentials
config.json
*.local.json
.env
.env.*

# local CodeGraph index
.codegraph/

# local MCP install config (machine-specific absolute paths)
.mcp.json

# local agent tooling (OpenSpec CLI commands/skills, per-machine, auto-generated)
.claude/
.agents/
